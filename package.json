{
  "name": "prime-trade-portal",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"pnpm --filter prime-trade-portal-server run dev\" \"pnpm --filter prime-trade-portal-client run dev\"",
    "build": "pnpm --filter prime-trade-portal-client run build && pnpm --filter prime-trade-portal-server run build",
    "seed": "pnpm --filter prime-trade-portal-server run seed"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}